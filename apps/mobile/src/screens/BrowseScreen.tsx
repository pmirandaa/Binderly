import { PlaceholderScreen } from './PlaceholderScreen';

import type { ReactNode } from 'react';

export function BrowseScreen(): ReactNode {
  return (
    <PlaceholderScreen
      title="Browse"
      description="Sets, cards, search and filters land here."
      ownerTask="T-M-BROWSE"
    />
  );
}
