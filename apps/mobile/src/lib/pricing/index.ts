// Barrel for the pricing-feature data layer (T-M-API-V2-WIRING).
// Internal-to-mobile (only consumed by `src/screens/card/` and the
// per-feature component bundle).

export { PRICING_QUERY_KEYS, useCurrentPriceQuery } from './hooks.js';
export type {
  UseCurrentPriceQueryOptions,
  UseCurrentPriceQueryResult,
} from './hooks.js';
