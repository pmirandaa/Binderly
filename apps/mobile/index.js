// Expo entry point. expo-router's `entry` shim handles the rest:
// it parses `app/` as the route tree and registers the root
// component with React Native's `AppRegistry`.
//
// Keep this file tiny and untouched by feature work.

import 'expo-router/entry';
