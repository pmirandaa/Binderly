// Expo Router entry for `/settings/shareables`. Thin re-export of
// the route component (which owns the auth gate + api wiring).
// Pattern matches `app/(tabs)/collection.tsx`.

import { ShareablesSettingsRoute } from '../../src/screens/shareables/ShareablesSettingsRoute';

export default ShareablesSettingsRoute;
