// Tab route — re-points to the real scan screen now that
// T-SC-CAMERA has shipped the camera + frame-processor
// infrastructure. T-M-SHELL's PlaceholderScreen comment
// documents the swap convention.

import { ScanScreen } from '../../src/screens/scan/index.js';

export default ScanScreen;
