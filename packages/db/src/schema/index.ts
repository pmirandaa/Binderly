// Schema barrel — Phase 1 schema tasks populate this with table
// definitions (e.g. `export * from './cards.js';`). The empty `export {}`
// keeps this file a valid ES module under `verbatimModuleSyntax`-style
// settings while no real exports exist yet.

export {};
