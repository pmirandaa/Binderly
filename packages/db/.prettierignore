# Build outputs and generated SQL never go through prettier — drizzle-kit
# owns the migration file format and tsc owns dist/. The repo-root
# `.prettierignore` already lists these; this local copy makes
# `pnpm --filter @binderly/db format:check` work the same way as a root
# `prettier --check`.
dist/
node_modules/
src/migrations/
__test__/
